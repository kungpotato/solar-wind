{
  "name": "arc-risk-score-worker",
  "version": "0.1.0",
  "private": true,
  "description": "Pay-per-call Arc wallet risk-score API. Hono on Cloudflare Workers, x402 client-broadcast settlement (no facilitator) via x402-arc.",
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "tail": "wrangler tail",
    "typecheck": "tsc --noEmit",
    "test": "vitest run"
  },
  "dependencies": {
    "@x402/core": "^2.13.0",
    "@x402/hono": "^2.13.0",
    "hono": "^4.7.1",
    "viem": "^2.21.26",
    "x402-arc": "^0.1.0"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^5.20260921.1",
    "typescript": "^5.6.0",
    "vitest": "^2.1.0",
    "wrangler": "^4.0.0"
  }
}
